[build]
  # 显式指定 Postgres schema（Netlify 生产用）
  command = "npx prisma generate --schema=prisma/schema.prisma && npx prisma migrate deploy --schema=prisma/schema.prisma && npm run build"
  publish = ".next"

# Next.js 16 在 Netlify 上零配置自动适配（OpenNext adapter）
# 不需要额外 plugin，Netlify 自动识别 Next.js 项目
